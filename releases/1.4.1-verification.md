# CubeOffice 1.4.1 release verification

Build revision: `98c9219ec1720a7d0592d405e1399ef492be20d6`

ALS Office revision: `03592d49ebb8d9f5757762f66b951369338148ff`

Android build: `1004001`

The four platforms consumed the same committed source archive. Platform receipts record exact output sizes and SHA-256 hashes. Public download identities and hashes are recorded in `1.4.1-report.json`.

Published and verified at: `2026-09-09T13:54:44.347683+00:00`

## Passed checks

- Native package versions, desktop architectures, and CLI smoke checks.
- Three desktop updater signatures validated cryptographically with the existing public key.
- Android release certificate continuity against the exact previous APK identified by the live feed.
- Targeted Windows frozen-frontend path tests and the previously merged Windows startup fix.
- Release transaction suite: 12 tests passed from the original repository layout.
- Staged and live desktop and Android feed checks, including the actual Android client validator.
- All 11 public artifacts and 7 website/feed files passed server SHA-256 and HTTPS checks.

## Limits and unresolved checks

- The supplemental headless Chrome test using packaged Android file URLs failed to observe DOCX preview persistence within 60 seconds. The document itself rendered successfully. An earlier test incorrectly reported persistence before the database write; that result is not counted as a pass.
- No Android device was connected for native confirmation. The Windows VM was locked during the final audit; the existing diagnostic GUI verification of the startup fix does not substitute for GUI testing of every final installer.
- macOS uses ad-hoc signing and is not notarized. Windows has no Authenticode certificate. Desktop updater artifacts are signed separately and their signatures passed verification.

See the published release report for final remote SHA-256 and public HTTPS verification results. The immutable build worktree is retained separately from this metadata commit.
